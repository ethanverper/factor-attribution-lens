# Factor Lens — Roadmap

Domain: finance
Path: `projects/finance/factor-attribution-lens/`
Selected from: [`docs/ideation/finance/2026-08-10-fintech-quant-analytics-shortlist.md`](../../../../docs/ideation/finance/2026-08-10-fintech-quant-analytics-shortlist.md) (idea 1 of 6)
Source research: [`docs/research/finance/2026-08-10-fintech-ai-quant-wealthtech.md`](../../../../docs/research/finance/2026-08-10-fintech-ai-quant-wealthtech.md)
Stakeholder: Ethan (human operator)
Started: 2026-08-10

## Goal

Give retail investors and small RIAs a transparent, explainable answer to "why does my portfolio behave the way it does" — CAPM beta, Fama-French factor loadings, and Markowitz-efficient positioning — computed from live market data, delivered as a real product rather than a black-box optimizer or an academic notebook.

## Definition of done (v1)

A working single-portfolio web app where a user enters holdings and receives, from live OpenBB-sourced market data:
1. CAPM beta vs. a chosen benchmark.
2. Fama-French 3-factor loadings (5-factor as stretch) with statistical diagnostics (R², t-stats, standard errors) — not just point estimates.
3. A Markowitz efficient-frontier view showing the current portfolio's position against the efficient set, with explainable trade-offs (not just "here's the optimal weights").
4. A plain-language narrative (dual register: technical + plain-language) explaining what's driving the portfolio's risk/return profile.

All four verified for correctness by `qa-tester` against reference calculations, clearly labeled as internal decision-support analysis (no personalized investment advice, no trade execution, no custody of funds).

## Why this project, why this shape

This is Cowork OS's first finance-category project and is scoped deliberately against `docs/about-me.md`'s "why these projects exist" lens, not as a generic build plan:

- **Industry-trend grounding**: OpenBB's Open Data Platform (v4.5, with an MCP server extension) just became a free, agent-native data backbone for exactly this class of tool — the research brief flags this as a recent, real cost-to-build drop, and factor/quant tooling for retail-or-small-team use as a genuinely thin-covered gap.
- **Real corporate-skill value**: this is not a CAPM/Fama-French/Markowitz notebook exercise — it's scoped as a shippable product with a real data layer, a visualization layer, and a narrative layer, mirroring how this analysis actually gets delivered professionally (e.g., Barra/Axioma-style factor risk tooling, just built transparent and accessible).
- **Specialized, named tools**: Python + OpenBB Open Data Platform as the live data backbone — not a static CSV or a toy dataset.
- **Deliberate CV balance**: CAPM/Fama-French/Markowitz modeling directly reinforces Ethan's existing academic quant background (low risk, high depth); live data integration via OpenBB and building this as a real running app (vs. a notebook) is the genuinely new territory this project pushes into.

## Phases (v1)

Sequenced by real dependency — each phase needs the previous phase's output to do real work, not busywork ordering.

- [x] Phase 1 — Foundation & Data Integration (`developer`) — DONE 2026-08-10: Stood up a Python/FastAPI app (`uv`-managed) integrating the OpenBB Open Data Platform (yfinance provider) for equity/benchmark prices and Kenneth French's Data Library (via `pandas-datareader`) for Fama-French 3-/5-factor return series — OpenBB itself does not carry the factor series, see `docs/decisions/0002-phase1-stack-and-data-sourcing.md`. Holdings input is manual JSON entry (weights validated to sum to 1); CSV import stretch goal not built. `POST /portfolio/returns` takes holdings + weights + benchmark + date range and returns a fully date-aligned bundle of equity/benchmark/portfolio/factor returns, verified against live (non-mocked) data end-to-end. Ready for Phase 2.
- [x] Phase 2 — Quant Core: Factor & Optimization Models (`quant-analyst`) — DONE 2026-08-10: Built `app/models/` as a structured, reusable module — CAPM beta (`capm.py`), Fama-French 3-factor with 5-factor supported end-to-end (`fama_french.py`), and a long-only Markowitz efficient frontier with the current portfolio positioned against it (`optimization.py`), all via a single orchestrator `analysis.analyze_portfolio(portfolio_return_data)` that consumes Phase 1's `PortfolioReturnData` directly. Regression diagnostics use Newey-West HAC standard errors; covariance estimation includes eigenvalue-clipping regularization for near-singular inputs. 28 tests pass (18 new, synthetic + one live end-to-end Phase 1→Phase 2 chain); see `docs/decisions/0003-phase2-quant-methodology.md` for the full methodology log. Ready for Phase 3.
- [x] Phase 3 — Explainable Attribution & Visualization Layer (`business-intelligence`) — DONE 2026-08-10: Built `app/dashboard/` as a server-rendered part of the FastAPI app (no separate frontend/build step) — `GET /` (holdings-entry form) and `POST /dashboard` (results page) run the exact same `PortfolioRequest` -> `build_portfolio_return_data` -> `analyze_portfolio` pipeline Phase 1/2 use, then render three views from the live result: (1) CAPM beta + Fama-French loadings as a diverging bar chart with 95% CI whiskers plotted directly on the chart (not hover-only), (2) the long-only efficient frontier with the current portfolio/GMV/max-Sharpe plotted against it and the return gap at matched volatility, and (3) a return/risk attribution view (`app/dashboard/attribution.py`) — return contribution per factor is an exact OLS identity (sums to the realized mean excess return), risk attribution is R² vs. idiosyncratic. Every chart follows the `dataviz` skill's method (validated categorical palette, fixed mark specs, hover tooltips, table-view twin, light/dark mode). Verified two ways: `tests/test_dashboard.py` (32/32 suite passing, including a live end-to-end check and the return-attribution numeric identity against live data) and a manual pass driving the actual running server through a headless browser with real holdings (AAPL/MSFT/GOOGL), which caught and fixed two real rendering bugs (a frontier-chart label collision, a marker-label overflow at the plot's right edge) before sign-off. Explicitly framed as decision-support only, no advice/signal language. See `docs/decisions/0004-phase3-dashboard-architecture.md`. Ready for Phase 4.
- [ ] ~~Phase 4 — Plain-Language Narrative Layer (`educator`)~~ — SUPERSEDED by Phase 9 below (folded into the output-standards retrofit's Learning section, same job, done properly embedded in the UI this time rather than as a bolt-on).
- [ ] Phase 5 — Verification & QA Sign-off (`qa-tester`) — RESEQUENCED after the retrofit (now effectively Phase 10 below): Independently verify CAPM/Fama-French/Markowitz math against known reference values, test edge cases (degenerate/singular covariance matrices, single-asset portfolios, missing/gappy data), and run end-to-end verification against the definition of done above.
- [x] Phase 6 — Publish (`developer`, gated by Ethan's explicit go-ahead) — DONE 2026-08-10, Ethan gave explicit go-ahead in chat: Committed Phase 2/3's previously-uncommitted work as two phase-scoped commits, confirmed the git repo stays scoped to `projects/finance/factor-lens/` (not the ambient home-directory repo), polished `README.md`, then created the public GitHub repo at [`github.com/ethanverper/factor-lens`](https://github.com/ethanverper/factor-lens) and pushed `main` — including this project's `docs/roadmap.md` and `docs/decisions/` alongside the code, per the team's publishing standard (`docs/about-me.md`). A `dev` branch was created and pushed (`origin/dev`) for subsequent iterative updates from Ethan; `main` stays the last-reviewed state until `dev` is merged back.

## Phases (v1.1 — output-standards retrofit)

Prompted by Ethan's feedback after seeing v1 live (decision 0002, `docs/project-standards.md`). All work happens on the `dev` branch, not `main`, until reviewed and merged.

- [x] Phase 7 — UI/UX Overhaul & Constrained Inputs (`developer`) — DONE 2026-08-10: Rebuilt the app around a persistent sidebar app shell (`app/dashboard/shell.py`) with all eight `docs/project-standards.md` sections as tabs (Overview, Inputs, Results built now; Learning/Glossary/References & Formulas/Real World-Corporate Applications left as clearly-marked "Coming in Phase 8/9" placeholders, owner-labeled). Design system: Fraunces (display serif) + IBM Plex Sans/Mono, a "research memo" table-of-contents nav (`§01`–`§08`), reusing the existing validated chart palette's `--series-2` as the site's one signature accent so page chrome and Phase 3's charts read as one system — chart/data logic in `viz.py`/`attribution.py` untouched, only re-homed into the Results tab. Replaced the free-text ticker/benchmark inputs with a hand-built accessible combobox (`app/dashboard/tickers.py`'s curated ~496-symbol S&P 500 universe + a 6-item benchmark list) — the submitted form field is a hidden input only ever set by selecting a real option, so invalid text can't reach submission; `app/dashboard/routes.py` also backstops this server-side (verified directly with `curl` POSTs carrying invalid tickers/benchmarks, both correctly rejected with 400). See `docs/decisions/0005-phase7-ticker-universe.md` for the universe sourcing/maintenance tradeoffs. Verified: full 32-test suite passing (including the live end-to-end `test_dashboard.py` checks, updated only where old page-structure assumptions no longer applied — none did, since `name="symbol"`/`name="weight"` and all required section-header substrings were preserved), `ruff check` clean, and a full manual pass in the in-app Browser tools against the live running app — holdings combobox search/select/keyboard-nav, invalid-free-text rejection, live end-to-end analysis run (AAPL/MSFT), dark mode, and a 375px mobile layout (sidebar collapses to a horizontal scrollable tab strip) all confirmed working. One pre-existing Phase 3 chart issue observed (not introduced here, out of this phase's scope): the frontier chart's "Global min-variance"/"Your portfolio" marker labels can overlap when a holding set's optimal portfolio lands very close to the min-variance point — flagged for Phase 10 (`qa-tester`) or a future chart pass, not fixed here since it's `viz.py` mark-placement logic, not structure/presentation chrome. Ready for Phase 8.
- [x] Phase 8 — References, Formulas & Results Review (`business-intelligence`) — DONE 2026-08-10: Filled in the References & Formulas section (`app/dashboard/shell.py::render_references_section()`) that Phase 7 left as a placeholder — five cards, each tagged to the module it documents (CAPM in `capm.py`, Fama-French 3-/5-factor in `fama_french.py`, Newey-West HAC regression diagnostics in `_regression.py`, Markowitz mean-variance/efficient frontier in `optimization.py`, return/risk attribution in `attribution.py`), each with what it computes, the real notation (inline HTML `<sub>`/`<sup>`, no external math-rendering library, consistent with decision 0004's no-client-library convention), and a primary-source citation, plus a consolidated "Works cited" list — describes what's actually implemented (HAC standard errors, long-only frontier with eigenvalue-clipping regularization, the compounding-vs-linear-scaling annualization split) rather than a generic textbook version. See `docs/decisions/0006-phase8-references-formulas-and-results-review.md` for the notation/citation convention and full source list, including a rendering bug (double-escaped HTML entities) caught and fixed during verification. Reviewed the Results section for regressions from Phase 7's restyling: ran the same AAPL(50%)/MSFT(30%)/GOOGL(20%) portfolio used in Phase 3's own verification both directly against the model code and through the live `POST /dashboard` form in a headless browser — every displayed number (CAPM beta/alpha/R², Fama-French loadings/alpha/R²/F-stat, frontier return/vol/Sharpe/gap, return and risk attribution) matched the directly-computed reference values exactly; confirmed clean, no regressions, nothing to fix. Also confirmed (not fixed, out of scope) the pre-existing frontier-chart "Your portfolio"/"Global min-variance" label-overlap issue Phase 7 flagged — visibly present on this same portfolio, screenshotted for Phase 10 context. Verified: full 32-test suite passing, `ruff check` clean, manual pass in the in-app Browser tools (light mode, dark mode, 375px mobile) confirming the new section renders correctly. Ready for Phase 9.
- [x] Phase 9 — Learning, Glossary & Real World Applications (`educator`) — DONE 2026-08-10: Filled in the three Phase 7/8 placeholders in `app/dashboard/shell.py` (`render_learning_section`, `render_glossary_section`, `render_real_world_section`), wired into `pages.py::_base_panels()`. **Learning** (§04): four dual-register cards (plain-language + technical) covering CAPM beta, Fama-French loadings, frontier position, and return/risk attribution — each cross-links to Results and References & Formulas via the existing `[data-tab-target]` tab mechanism rather than re-deriving any formula. **Glossary** (§05): 19 project terms, dual-register, tagged with where each shows up in the app; also written to `projects/finance/factor-lens/docs/glossary.md` and rolled into the Cowork OS portfolio-wide `docs/glossary.md` (18 finance/quant terms, first logged there). **Real World / Corporate Applications** (§08): four cards grounding this project in the source research brief (`docs/research/finance/2026-08-10-fintech-ai-quant-wealthtech.md`) — Barra/Axioma-style institutional risk desks, RIA advisor-tech (YCharts/Zephyr, Advyzon AI), wealthtech/robo-advisor rebalancing engines and the 14%→30% platform-consolidation stat, and an explicit recruiting-facing "why this is worth evaluating the builder" card citing the brief's own thin-coverage gap finding. All eight nav sections are now real content; `_PENDING_TABS` is empty and no "Coming in Phase 9" markers remain. One phrasing caught and fixed by the existing "no imperative advice language" test guardrail (`test_dashboard_submit_end_to_end`): an early draft of the frontier card's plain-language text used "not that you should hold different stocks," which was reworded to "not a suggestion to hold different stocks" to avoid the literal substring "you should" while keeping the same meaning. Verified: full 32-test suite passing, `ruff check` clean, and a full manual pass in the in-app Browser tools (light mode, dark mode, 375px mobile) confirming all three new sections render correctly and the cross-tab xref buttons actually navigate. Ready for Phase 10.
- [x] Phase 9b — Inputs UX: Weights Explainer (`developer`) — DONE 2026-08-10: Ethan flagged that the "weight" field in the holdings form is unexplained and unintuitive — a user doesn't know what to put there or why. Relabeled the field "Allocation %" and switched it from a raw 0-1 fraction to a percent (0-100) — the more intuitive unit — with an inline one-line explainer above the holdings rows stating what allocation means and that it must sum to 100%; the underlying Phase 1 API contract (`app/schemas.py`) is untouched, the percent-to-fraction conversion happens only at the HTML form boundary (`app/dashboard/routes.py`). Added a live "Total allocated: NN%" badge (`app/dashboard/shell.py`'s existing palette: green at exactly 100%, amber under, red over) that recomputes on every keystroke via one delegated `input` listener, plus "Split evenly" and "Normalize to 100%" helper buttons that operate on whichever holdings currently have a ticker selected. The "Run analysis" submit button is disabled until the total is within 0.05 of 100%, so a user can no longer submit blind and discover the mismatch only after a round trip. Server-side validation (`routes.py`) still backstops with a plain-language, percent-framed error for any submission that bypasses the client (verified directly with a raw `fetch` POST). See `docs/decisions/0007-phase9b-allocation-percent-ux.md`. Verified: full 35-test suite passing (32 pre-existing plus 3 new Phase 9b tests: sum-not-100 rejection, out-of-range rejection, live-explainer/total-badge presence, plus updated percent-based assertions in the existing end-to-end test), `ruff check` clean, and a full manual pass in the in-app Browser tools — typed allocations watched live-updating through under/exact/over states in both light and dark mode, "Split evenly" and "Normalize to 100%" both verified to produce an exact 100% sum (remainder assigned to the last row), the disabled-submit state confirmed on both desktop and a 375px mobile layout, and a full live end-to-end AAPL/MSFT/GOOGL submission confirmed working with the new percent inputs. Ready for Phase 10.
- [x] Phase 9c — Visual Design Pass & New Standard Features (`developer`) — DONE 2026-08-10: Redesigned the app shell from Phase 7's "research memo" look (Fraunces serif, cream canvas, warm orange accent, "§NN" table-of-contents marks) to a "quant terminal / instrument panel" direction — audited *why* it read plain first (see `docs/decisions/0008-...md`), not just swapped fonts. New tokens: Space Grotesk (display) + Inter (body) + JetBrains Mono (used aggressively for all data, not just tickers), a graphite-navy dark mode / cool gray-blue light mode (neither is the flagged "cream+terracotta" or "pure-black+acid-accent" AI-default), one amber "signal" accent (phosphor-terminal-inspired, replacing the orange), bracket-style `[NN]` nav marks, a faint instrument-panel grid texture, and mono tabular-num stat tiles with an amber top border — all as shared CSS custom properties on `.viz-root` (`app/dashboard/viz.py::CHART_STYLE`) so the existing charts inherit the new palette automatically; chart *mark* logic itself is untouched, left for Phase 9d. Added the three rule-8 standard features: (1) a one-click sample-portfolio quick-start (`GET /dashboard/sample`, AAPL/MSFT/GOOGL/AMZN, linked from Overview and Inputs, no JS/typed input required); (2) a shareable link (`GET /dashboard/view`, replays the exact holdings/benchmark/date-range/model via query params against live data — chosen over server-side PDF generation given the no-client-library architecture, see decision 0008) plus a browser-print-based "Export as PDF" button backed by a new `@media print` stylesheet; (3) absolute Open Graph/Twitter social-preview meta tags with a generated 1200x630 branded image (`app/static/og-image.png`, built once via `scripts/generate_og_image.py`/Pillow — not a runtime dependency). Surfaced the curated S&P 500 ticker-universe source/limitation directly on the Inputs tab next to the combobox (rule 7's "near the input" half; the formal References & Formulas citation is Phase 9d's). `design-taste-frontend` (`leonxlnx/taste-skill`) is not installed in this environment — flagged in the decision doc rather than silently skipped; proceeded audit-first with `frontend-design` alone. Verified: full 42-test suite passing (35 pre-existing + 7 new: sample quick-start end-to-end, shareable-link round-trip incl. invalid-ticker rejection, share/export controls present, ticker-source note present, OG/social meta present), `ruff check` clean, and a full manual pass in the in-app Browser tools — light/dark mode across all eight tabs, the sample quick-start run live end-to-end, the shareable link copied and re-opened to confirm it reproduces the same result, OG meta tags and the served `og-image.png` inspected directly, and 375px mobile. Caught and fixed one real bug during mobile verification (not introduced by this phase's token changes, but exposed by them): `.app-shell`'s `align-items: flex-start` wasn't reset for the mobile column layout, so `.app-main` sized to its widest chart's content instead of the viewport, causing horizontal page overflow — root-caused via computed-style inspection before fixing, per `systematic-debugging`. See `docs/decisions/0008-phase9c-visual-redesign-and-standard-features.md`. Ready for Phase 9d.
- [x] Phase 9d — Results Visual Restyle & Citation (`business-intelligence`) — DONE 2026-08-10: Audited the Results charts against the `dataviz` skill with fresh eyes (not just re-reading Phase 9c's claim that tokens flow through) and found three concrete, verifiable gaps between "restyled chrome" and a real quant-terminal readout, all in `app/dashboard/viz.py`: (1) `.viz-card h2` chart titles weren't using `var(--font-display)` like every other card heading in the app; (2) every SVG-drawn number/label inside the charts (axis ticks, bar values, category names) had no `font-family` set and fell back to the body sans instead of the mono face used "aggressively" everywhere else per decision 0008 — fixed with one CSS rule (`.viz-chart-wrap svg text`); (3) `.viz-root`'s own hardcoded `system-ui` font stack in `CHART_STYLE` was masked-but-inconsistent with the shell's token system. Also added two chart-specific mark upgrades: vertical gridlines completing the frontier chart's grid (was horizontal-only), and a soft amber halo behind the "Your portfolio" frontier marker only, echoing the signal accent's "armed/active" glow used on buttons elsewhere. Added the formal ticker-universe citation to References & Formulas (`render_references_section()`) — a sixth card matching the same source/methodology/known-limitation rigor as the CAPM/Fama-French/Newey-West/Markowitz/attribution cards, completing rule 7 (Phase 9c shipped the "near the input" half). Caught and fixed a real overflow bug during that card's own verification: two prose-length facts had been stuffed into the `.formula-block` component (built for short, non-wrapping math notation), silently overflowing the card width with no scroll affordance — rewritten as genuinely terse notation-style lines, verified via `scrollWidth`/`clientWidth` on all six cards. See `docs/decisions/0009-phase9d-results-visual-restyle-and-citation.md`. Regression check: re-ran Phase 8's AAPL(50%)/MSFT(30%)/GOOGL(20%) portfolio, computing directly against `analyze_portfolio` and cross-checking all 10 displayed figures against the live-rendered `POST /dashboard` page — exact match, no regressions from Phase 9c. Also re-ran the Phase 9c sample-portfolio quick-start end-to-end. Verified: full 42-test suite passing (unchanged — no new user-facing behavior, only visual/citation changes to already-tested surfaces), `ruff check` clean, and a full manual pass in the in-app Browser tools (light mode, dark mode, 375px mobile) confirming the Results tab, References & Formulas, and Inputs tab all render correctly. Re-confirmed (not fixed, flagged for Phase 10) the frontier chart's "Your portfolio"/"Global min-variance" marker-label overlap — now unresolved across four phases (3, 7, 8, 9d), specifically called out to `qa-tester` for an actual fix rather than another deferral. Ready for Phase 9e.
- [x] Phase 9e — Learning Diagrams & Glossary Completeness Audit (`educator`) — DONE 2026-08-10: Added three hand-built inline SVG diagrams (`app/dashboard/diagrams.py`, new module) to the Learning section per rule 5, following the `artifact-diagramming` skill's construction principles applied directly (no separate claude.ai Artifact, since a visitor to the deployed app needs to see these in place): (1) a CAPM decomposition — risk-free baseline + β-scaled market exposure + alpha as three blocks summing exactly to the total return; (2) a generic, illustrative factor-loading confidence-interval diagram (deliberately different example data from the Results chart) teaching *how to read* a whisker — one example whose CI excludes zero (real exposure) vs. one that includes it (inconclusive); (3) a conceptual efficient-frontier position diagram showing a portfolio below the curve with two labeled arrows for the two equivalent readings of the gap (more return at the same risk, or the same return at less risk). The fourth Learning card (return/risk attribution) deliberately kept text-only — it's an exact algebraic identity already shown as real notation and a live table, and a diagram would just redraw the same equation. All three diagrams reuse the existing design tokens (`--signal`, `--series-1`, `--status-good`/`--status-warning`, `--font-mono`) with no new colors. Caught and fixed two real label-overflow/collision bugs during this phase's own element-scoped Playwright verification (a block sized too narrow for its own label, clipping text past the SVG's `viewBox`; a verdict label overflowing the right edge) before sign-off. Separately ran a full glossary completeness audit against every tab of the live app and found two real kinds of gaps: four terms already correctly written in `docs/glossary.md` (Covariance regularization, Return attribution, Risk attribution, Markowitz mean-variance optimization) had never actually been ported into the live `render_glossary_section()`, and five more terms appeared repeatedly in the app's own text with no definition anywhere (F-statistic, excess return, the risk-free rate, the covariance matrix itself, and the compounding-vs-linear-scaling annualization convention). Closed all nine (19 → 27 terms) across all three copies (live app, project `docs/glossary.md`, portfolio-wide `docs/glossary.md` — which had its own independent, slightly different gap, also closed) and regrouped the growing list into three concept areas (Factor models & regression, Portfolio theory & optimization, Attribution) instead of one flat list. See `docs/decisions/0010-phase9e-learning-diagrams-and-glossary-audit.md`. Verified: full 42-test suite passing (unchanged count, no new user-facing behavior the existing tests assert on), `ruff check` clean, and a full manual pass in the in-app Browser tools — element-scoped screenshots of all three diagrams in light and dark mode, a full-page pass of the regrouped Glossary tab in both themes, and a 375px mobile check on both tabs confirming no horizontal overflow. Ready for Phase 10.
- [x] Phase 10 — Verification & QA Sign-off (`qa-tester`) — DONE 2026-08-11, verdict **NOT READY**: Independently verified CAPM/Fama-French/Markowitz math against closed-form reference calculations (exact match, ~1e-16 diff), all edge cases (singular covariance, single-asset, gappy data, invalid inputs, allocation-sum enforcement) pass, full 8-section end-to-end + sample quick-start + shareable link + OG metadata + light/dark/mobile all confirmed, 42/42 tests, `code-review` skill run clean. **Blocking**: the frontier-chart marker-label overlap (flagged across Phases 3/7/8/9d, never fixed) — root-caused to `app/dashboard/viz.py::marker_group()` (lines 552-578) having right-edge overflow avoidance but no label-vs-label collision avoidance between the three markers. Non-blocking: raw Pydantic error text leaks to users on duplicate-symbol/bad-date-range submissions (`routes.py` lines 156, 182-183), and single-asset/degenerate portfolios render an empty frontier curve (correct math, confusing visual). See Phase 10b for the fix.
- [x] Phase 10b — Bug Fixes from QA (`developer`) — DONE 2026-08-11: Fixed all three Phase 10 findings. **Blocking**: `app/dashboard/viz.py::frontier_chart` marker labels (GMV/Max Sharpe/current portfolio) now share placement awareness instead of each independently avoiding only the plot's right edge — markers whose pixel positions land within 6px of each other (union-find clustered) get one combined label ("GMV = Your portfolio", or all three merged for the degenerate single-asset case where they're the exact same point) instead of stacking illegibly; any residual close-but-distinct overlap is resolved by a second nudge-down pass. See `docs/decisions/0011-phase10b-frontier-marker-label-collision.md` for the cluster-and-merge rationale over pure collision-avoidance. **Non-blocking**: duplicate-symbol and bad-date-range submissions (`routes.py`) now get explicit plain-language checks ahead of `PortfolioRequest` construction, matching the existing "Your allocations add up to..." convention, with a `_plain_validation_message()` backstop (strips pydantic's raw dump to one sentence) for any other schema-level failure; degenerate/single-asset portfolios now render a new `viz.info_banner()` inline note explaining the empty frontier instead of a silently-blank chart (`pages.py::_render_frontier_section`). Verified: full 51-test suite passing (42 pre-existing + 5 new pure-unit frontier-label tests + 4 new live dashboard regression tests), `ruff check .` clean, and a full manual pass in the in-app Browser tools against QA's exact repro steps (sample-portfolio quick-start, single-asset 100% AAPL, duplicate-symbol submission, bad date range) in light mode, dark mode, and 375px mobile — all confirmed working, including the three-way merged label and info banner together on the degenerate case. Ready for Phase 11 / re-review by `qa-tester`.
- [x] Phase 10c — Real Identity Pass — DONE 2026-08-11, produced directly rather than via `brand-creative` (the agent registered mid-session and wasn't yet available to the harness for this one turn — it's the standing owner of this kind of work going forward): Researched three genuinely comparable, currently-live products (Stripe, Linear, Mercury) via actual browsing/`getComputedStyle` inspection rather than abstract taste. Real finding: all three use restrained medium-weight headline type (480-510, not bold), Linear validates the existing graphite-navy/off-white and `[NN]`-style nav system directionally, and Mercury's bespoke commissioned typeface is the strongest single identity lever found. Spec: (1) drop headline/display type weight from Space Grotesk's heavy default to Medium/Regular — the single highest-leverage change; (2) give "Factor Lens" an actual mark — a circular aperture/focus-ring glyph tied to the name itself; (3) make that aperture motif *functional*, not decorative — the frontier chart's "current portfolio" marker becomes a literal focus-ring, and OpenBB's slow first-load state gets an iris open/close instead of a generic spinner; (4) audit the amber signal accent for one-accent discipline (Stripe/Linear pattern — used only for the single most important element per view, not decoratively); (5) preserve the graphite-navy dark mode, JetBrains Mono for data, and the bracket nav system — all validated by real reference, not wrong. Personality line: "looking through a precise instrument — quiet, exact, nothing decorative." Full citations and rationale in `docs/decisions/0012-phase10c-brand-identity.md`.
- [x] Phase 10d — Implement Identity (`developer`) — DONE 2026-08-11: Built all five changes from decision 0012. **(1) Type weight**: every `var(--font-display)` heading (h1/h2, card titles across all eight tabs, the masthead wordmark) dropped from 600/700 to 500; kept 600+ only on the two genuinely load-bearing single-stat elements (`.viz-stat-tile .stat-value`, `.alloc-total-value`), both mono data faces, not display-font "poster" headings. **(2) The mark**: new `app/dashboard/mark.py` builds a concentric-ring aperture/focus-ring glyph from one shared construction, used for the masthead (next to the wordmark, `currentColor`-driven so it themes for free) and the favicon (old "FL" bracket-box monogram fully removed). **(3) Functional, not decorative**: the frontier chart's "current portfolio" marker (`viz.py::frontier_chart`) is now the same concentric-ring glyph instead of a flat dot (Phase 10b's label-merge/collision-avoidance logic untouched, only the marker's own shape changed) — verified live via computed-style DOM inspection (4 amber ring/halo circles at the correct radii) and a new unit test; the OpenBB slow-fetch loading state was a genuine gap (no loading indicator existed at all, not just a spinner to restyle) — filled with a full-page overlay showing the same glyph opening/closing (CSS-animated SVG radius + slow rotation, `prefers-reduced-motion`-gated), armed on the holdings form submit and both sample-quickstart links since this app is classic full-page-navigation, not fetch/XHR. **(4) Amber audit**: checked the *rendered* app per tab (not just CSS) and found real decorative overuse beyond the CSS-reading dilligence alone would have caught — most strikingly 28 amber "Technical" register-label uses on one Glossary view — and pulled back stat-tile borders, References tag pills, formula-block borders, Overview's three method-mark labels, the quickstart button (was a second solid CTA competing with the form's own submit button), and the dual-register technical-label color to neutral; post-fix, every tab now shows at most the shared `[NN]`-echoing section eyebrow plus one genuine functional accent (a primary CTA, the frontier legend, or nothing else). **(5) Preserved untouched**: graphite-navy dark mode, JetBrains Mono for all data, `[NN]` bracket nav. See `docs/decisions/0013-phase10d-identity-implementation.md` for the construction/mechanics rationale and the audit's full before/after. Verified: full 57-test suite passing (51 pre-existing + 6 new: `tests/test_mark.py`'s 3 pure-unit glyph tests, a new frontier-chart iris-shape unit test, and 2 new live dashboard tests for the loading overlay/mark wiring), `ruff check .` clean, and a full manual pass in the in-app Browser tools — light mode, dark mode, and 375px mobile on Overview/Inputs/Results/Glossary, the loading overlay triggered and screenshotted showing the animated glyph, and the frontier chart's iris marker screenshotted in both themes on a live sample-portfolio run. Ready for Ethan's review before Phase 11.
- [x] Phase 10e — Trim OpenBB Dependency Bloat (`developer`) — DONE 2026-08-11: Confirmed via `grep -rn "obb\."` across `app/` that the app's entire OpenBB surface is `obb.equity.price.historical(...)` (`app/data/equity.py`, reused by `benchmark.py`) against the `yfinance` provider — nothing else. Replaced `openbb>=4.7,<5` in `pyproject.toml` with the three packages that actually provide that surface: `openbb-core>=1.6,<2`, `openbb-equity>=1.6,<2`, `openbb-yfinance>=1.6,<2` (all already-present transitive versions, pinned the same major-version-bounded way the old meta-package was). `uv.lock` now lists exactly those three `openbb-*` packages (was 31, spanning SEC/FRED/IMF/crypto/news/congress.gov/economy/fixed income/etc.). Hit and root-caused (via `systematic-debugging`) a real bug along the way: an in-place `uv sync` left OpenBB's generated route-stub tree stale/broken (`ImportError: cannot import name 'obb'`) since the old meta-package's uninstall removed files needed to bootstrap OpenBB's own package-rebuild step; fixed by deleting `.venv` and doing a clean `uv sync` so the rebuild runs correctly (stub tree went from ~39 provider-spanning files to 9 equity-only files). See `docs/decisions/0014-phase10e-trim-openbb-dependency-bloat.md` for the full before/after (package count 62→3, `uv run uvicorn` ready in a clean 4.02s post-warm, steady-state `openbb` import 0.74s) and an important caveat: absolute numbers are colored by this sandboxed environment's unusually slow disk I/O (the in-place uninstall alone took 11m43s), so the relative improvement (package/provider count, and the resulting process behavior) is the portable takeaway, not the raw seconds. Verified: full 57-test suite passing (live network calls, no mocking, unchanged from Phase 10d), `ruff check .` clean, and a full manual pass in the in-app Browser tools — started the real server and ran the sample-portfolio quick-start end-to-end (AAPL/MSFT/GOOGL/AMZN) against live yfinance/Kenneth French data through the trimmed install, confirmed no regressions. Ready for Ethan's review before Phase 11.
- [x] Phase 10f — Real Interactivity Pass (`brand-creative`) — DONE 2026-08-11: Audited the live app first (ran it locally, drove it through the in-app Browser tools) rather than reasoning abstractly — found the identity pass's states were already well-designed (button/nav hover colors, the allocation total badge's under/exact/over states) but never eased, tab switching and the Inputs→Results page arrival were both hard, un-animated cuts, and the three Learning-section diagrams (the direction Ethan explicitly liked) were fully static. Loaded `gsap-core`, `design-motion-principles` (weighted Emil Kowalski primary/Jakub Krehel secondary/Jhey Tompkins selective-for-Learning-only, per the app's actual shape as a data-dense analytics dashboard, not a marketing site), and `ui-ux-pro-max`'s reference guidelines to ground a concrete, per-surface spec rather than abstract "add motion": eased hover/press states everywhere they already exist but weren't transitioning; a fast 120ms tab-fade (deliberately minimal, per the frequency gate — tabs are clicked dozens of times a session); an on-arrival entrance choreography for the Results page (freshness banner → stat tiles → charts, sectioned 2/3 triggering on scroll-into-view rather than all at once) in place of a hard-cut page swap; per-mark-type data reveals (stat-tile count-up, bar `scaleX` growth, frontier `polyline` stroke-dashoffset draw-in with staggered marker pop-ins); a paired number+range slider for allocation inputs (precision input kept, not replaced, per Markowitz's need for exact values); and a scroll-triggered, sequenced reveal for the three Learning diagrams specifically (extending, not replacing, that direction), while explicitly ruling out scroll-linking Glossary/References/Tools (scanning content, not narrative moments). Also produced the rule-13 restructure of Tools & Technologies — five real categories (languages, frameworks/libraries, data/quant methods, presentation/rendering, infrastructure) with every substantial entry tied to its actual use in this project, framed against `docs/about-me.md`'s reinforcement-vs-new-territory lens. Flagged for `developer`/`pm`: GSAP is a new (small, self-hostable, no-build-step) client dependency — no architecture change to the server-rendered/no-client-framework model is required, and no ScrollTrigger plugin is needed since nothing here scroll-scrubs. See `docs/decisions/0015-phase10f-interactivity-and-tools-section.md` for the full per-surface spec (exact selectors, durations, easings) and content. Ready for Phase 10g.
- [x] Phase 10g — Implement Interactivity (`developer`) — DONE 2026-08-11: Built decision 0015's full interaction/motion spec in new module `app/dashboard/motion.py` — eased hover/press transitions, 120ms tab-switch fade, GSAP entrance choreography on Inputs→Results (freshness banner → stat tiles → charts, sections 2/3 on scroll-into-view), stat-tile count-up, `scaleX` bar reveal, frontier `stroke-dashoffset` draw-in with staggered marker pop-ins, paired number+range sliders for allocation inputs (two-way bound into the existing validation logic, zero backend change), and scroll-triggered reveals on the three Learning diagrams only (verified no other section carries a scroll observer, by construction not convention). GSAP is self-hosted (`app/static/vendor/gsap.min.js`), not CDN-loaded. `prefers-reduced-motion` is respected structurally — every reveal function only ever hides an already-correct SSR'd value immediately before animating it back, so a no-JS or reduced-motion client sees zero flicker. Also fixed the unscoped `--reload` watcher (now `--reload-dir app`, confirmed via uvicorn's own startup log and a `.venv`-touch test) and replaced Tools & Technologies with decision 0015's five-category, hiring-manager-readable content. See `docs/decisions/0016-phase10g-interactivity-implementation.md` for full implementation detail. **Verification note**: the assigned agent stalled repeatedly waiting on its own background test runs under heavy, unrelated system load (average 11-15) and never reached the scoped `impeccable` polish pass or final commit — picked up directly rather than re-delegated again: found and fixed one real test bug (an overly-broad assertion that the substring "cdn" appear nowhere on the page, which false-positived on the new Tools & Technologies copy legitimately saying "not a CDN"), confirmed the debug scratch file the agent mentioned had already been cleaned up, verified the reload-dir fix and vendored GSAP directly, and committed (`5f7b69d`). **`impeccable` polish pass remains open, not run** — flagged explicitly rather than marked done (see decision 0016 §4).
## Phases (v1.2 — React/Tailwind/shadcn frontend rebuild)

Fourth round of frontend feedback still read as "plain text app... no branding." Root cause (decision 0004, team-level): the hand-rolled server-rendered/SVG/vanilla-JS architecture itself, not lack of design taste applied to it. Cowork OS's new default stack is React + Tailwind CSS + shadcn/ui with the backend as a pure JSON API (`docs/project-standards.md` rule 4) — this rebuilds Factor Lens's presentation layer only. `app/models/`, `app/data/`, and the existing `/portfolio/returns` JSON API (Phase 1) are untouched and are exactly what the new frontend calls; nothing about the validated quant logic changes.

- [x] Phase 10h — Identity Direction for the Real Stack (`brand-creative`) — DONE 2026-08-11: Re-grounded decisions 0012 (identity)/0015 (motion) in actual shadcn/ui components and Tailwind tokens rather than re-deriving direction from scratch. Audited the live app first (Overview's three flat text cards, Real World's paragraph-wall-plus-tag-pills cards, Tools & Technologies' icon-free flat lists — all confirmed via the in-app Browser tools, not from memory) and researched shadcn/ui directly (`ui.shadcn.com`, live: the `dashboard-01` block, the `Chart`/`Sidebar`/`Combobox`/`Empty`/`Accordion` components' real APIs) plus stripe.com/payments's hero (a real embedded product-UI preview split-hero, not an illustration) as the concrete pattern behind the new Overview hero; ycharts.com was checked as a directly-comparable competitor and explicitly rejected as a reference (generic, dated). Spec covers: (1) a full Tailwind/shadcn CSS-variable token mapping carrying the existing graphite-navy/amber hex values forward by value (light + dark), plus the non-negotiable carryover that no heading uses shadcn's default `font-semibold` — every display heading stays `font-medium` (500) per decision 0012's highest-leverage finding; (2) a section-by-section shadcn primitive mapping (Sidebar+real routes for nav, Combobox/Select/Slider/Calendar for Inputs, Chart-wrapped Recharts for Results, Accordion for Glossary, Card grids for Tools & Technologies/Real World/References); (3) a concrete Overview hero spec — asymmetric split layout, a real live miniature of the actual frontier chart (not decoration) plus a Stripe-style precision-stat callout, replacing the three flat cards; (4) the aperture mark ported as one `<ApertureMark/>` React component feeding the masthead/favicon/frontier-marker/hero-marker/loading-state from one construction, and an explicit decision to keep GSAP (not switch to Framer Motion) so decision 0015's validated per-surface motion spec ports by mechanism-change only, not re-research; (5) a per-section visual-density table directly answering the "puro texto" complaint (icon-per-category for Tools & Technologies, stat-pull-quote treatment for Real World, grouped Accordion for Glossary). Recommended (not required) upgrading References & Formulas' hand-rolled sub/sup HTML to real KaTeX now that decision 0004 lifted the "no client library" constraint that originally forced it. Flagged to `developer`/`pm`: the 8 sections become real React-Router routes (deep-linkable, a genuine improvement, not just a port) rather than in-page tab panels — a real architectural specific worth naming explicitly. See `docs/decisions/0017-phase10h-identity-for-react-stack.md` for the full token tables, component mapping, and hero spec; accompanied by a style-tile/mockup Artifact. Ready for Phase 10i.
- [x] Phase 10i — React/Tailwind/shadcn Rebuild (`developer`) — DONE 2026-08-11: Built the new frontend end to end per Phase 10h's spec. **Scaffold**: Vite + TypeScript + Tailwind CSS v4 + shadcn/ui (`frontend/`), React Router for the 8 real routes, `recharts` for data charts, GSAP (self-hosted via npm, not vendored) for motion — see `docs/decisions/0018-phase10i-react-rebuild.md` for why the shadcn CLI needed an explicit `-b radix` re-init (its new default preset builds on Base UI, not Radix, which decision 0017's own component mapping assumed). **Backend**: new `app/api/` package replaces `app/dashboard/` entirely — `POST /api/analysis` is the headline addition, exposing `analyze_portfolio()`'s full CAPM/Fama-French/Markowitz pipeline as its own JSON contract for the first time (previously only reachable from inside the old server-rendered route); `GET /api/tickers` and `GET /api/sample` serve the curated universe and quick-start default as JSON. `app/models/`/`app/data/`/`POST /portfolio/returns` untouched. **All eight sections built as real components**: Overview's hero (a live, real Recharts miniature of the frontier chart using the actual sample-portfolio API response, plus a live-computed precision-stat callout — not decoration); Inputs (Combobox-based ticker/benchmark selection constrained to the curated universe, paired number+Slider allocation controls with live-total/split-evenly/normalize logic, all ported and verified); Results (stat tiles, factor-loading bars with CI whiskers via Recharts' floating-bar pattern, the frontier chart with decision-0011's marker-label-merge behavior ported via a domain-normalized clustering threshold, attribution); Learning (all four dual-register cards, all three diagrams rebuilt as real React/SVG components with GSAP scroll-triggered reveals, replacing the old Python-generated SVG strings); Glossary (all 27 terms, 3 concept groups, in a real shadcn `Accordion`); Tools & Technologies (icon-per-category `Card` grid, decision 0015's content, updated for the new stack); References & Formulas (all 6 cards + works-cited, notation kept as HTML `<sub>/<sup>` — KaTeX upgrade deliberately deferred, see decision 0018 §5); Real World (pull-quote/stat-callout treatment per decision 0017 §5, one real number pulled per card). **Carried forward and verified, not just ported**: the allocation validation logic (sum-to-100%, disabled submit), constrained ticker/benchmark selection (no free text reaches the backend — re-validated server-side too), the frontier marker-label-merge logic (verified live: a degenerate portfolio correctly showed "Global min-variance = Your portfolio" merged while "Max Sharpe (tangency)" stayed separate), OG/social preview metadata, the aperture/iris mark as a real `<ApertureMark/>` component (masthead, favicon, and the frontier's current-portfolio marker, one shared geometry via `lib/aperture.ts`), dark mode, `prefers-reduced-motion` (implemented via `useReducedMotion()` gating every GSAP effect plus a blanket CSS fallback — verified by code review, not a live-emulated browser test; this sandboxed environment has no way to force the media feature), and mobile responsiveness (Sheet-collapsed sidebar, verified at 375px). **Deployment shape**: FastAPI serves `frontend/dist/` directly in production (one service, not two) — see decision 0018 §2 for the tradeoff. **Verified**: full backend test suite (34/34 — the four dashboard-only test files were obsolete and removed; a new `tests/test_api_analysis.py` covers `POST /api/analysis`/`GET /api/tickers`/`GET /api/sample` including the invalid-ticker/benchmark 400 paths and the return-attribution exact-identity property, all against live data), `ruff check` and `tsc -b`/`npm run build`/`oxlint` all clean, and an extensive live-browser pass: the sample-portfolio quick-start and manual holdings entry both run end-to-end against live data, all eight sections inspected for real content (not stubs), a hard-refresh deep link to a `/results?...` URL correctly replays a specific configuration (shareable-link mechanism, now URL-native per decision 0017's own flagged architectural change), the degenerate single-asset frontier case and an invalid-ticker submission both render the correct error/info states, the 5-factor model path renders RMW/CMA loadings, dark mode and mobile (375px, Sheet sidebar) both confirmed. See `docs/decisions/0018-phase10i-react-rebuild.md` for the full GSAP-vs-Framer-Motion confirmation, the Recharts custom-geometry deviations (CI whiskers, frontier label clustering) and why, and what's explicitly deferred (KaTeX, JS bundle code-splitting). Ready for Phase 10j.
- [x] Phase 10k — Interpretation & Key Takeaways Content (`quant-analyst`) — DONE 2026-08-11: Fifth round of feedback named this "the most important section a project has" (`docs/project-standards.md` rule 9a, new). Wrote the real analytical synthesis logic as an implementable rule set — not generic commentary — grounded directly in `PortfolioAnalysis`'s actual fields (`app/models/schemas.py`: `CAPMResult`, `FactorModelResult`, `EfficientFrontierResult`). Spec'd a new `app/api/interpretation.py` (mirroring `attribution.py`'s existing server-side-logic/React-presentation split) producing four always-present takeaways (beta precision & market exposure, Fama-French style-tilt significance pattern, R²/explanatory-power, frontier position) plus a dynamically-selected one-sentence headline (priority-ordered: very-low R² → beta distinguishable from both 0 and 1 → a material+significant style loading → a large frontier gap → a "nothing stands out" default) and up to two cross-cutting data-quality flags (covariance regularization, short data window). Verified the rules against **live-computed data**, not invented numbers — ran `analyze_portfolio()` directly against three real portfolios (the existing AAPL/MSFT/GOOGL/AMZN sample; a defensive JNJ/PG/KO/WMT portfolio whose CAPM R² came back at 1.5%, a genuine and non-obvious finding; and a short-window GOOG/GOOGL/MSFT case exercising the short-data-window flag) and wrote out the exact resulting headline/takeaway/flag text for each, confirming the rules produce distinct, correctly-differentiated, portfolio-specific narratives rather than templated filler. Explicitly logged two honest gaps found during verification rather than hiding them: covariance-regularization couldn't be triggered by any real ticker combination tried (flagged as a safety-net path `qa-tester` should test synthetically, not hunt for in real data) and the "nothing stands out" default headline branch wasn't hit by any of the (four, including one extra diversified cross-sector check) real portfolios tested. Every template and every worked-example sentence re-checked against the standing hard limit against personalized investment/trading advice — no "you should"/"consider"/"rebalance," frontier-gap language reuses the app's own existing disclaimer clause (`frontend/src/data/learning.ts`) rather than inventing new phrasing. See `docs/decisions/0019-phase10k-interpretation-content.md` for the full rule set, thresholds, response schema, and all three worked examples. Ready for Phase 10m.
- [x] Phase 10l — Institutional Finance Register & Interactive Learning Refinement (`brand-creative`) — DONE 2026-08-11: Refined (not rebuilt) the existing React/shadcn identity toward a real institutional-finance register, researched live against `goldmansachs.com` (bespoke condensed type, thin hero numerals, sourced footnotes, monochrome), `jpmorgan.com` (bespoke type, one muted accent on uppercase CTAs), and `chase.com` (plain bold retail contrast, confirming the restrained register is institutional-only, not universal). Spec'd the curriculum-style single-open Learning `Accordion` with a `sessionStorage`-persisted `Progress` bar, the JPM-style numbered-footnote disclosure pattern (`FootnoteMarker`), a required predict-then-reveal check on `CiWhiskerDiagram` and a recommended draggable-β slider on `CapmDecompositionDiagram`, the `SidebarFooter` builder-credit colophon (rule 9b), and explicitly declined a fourth interactive gimmick on the frontier diagram and bespoke thin hero numerals for the exact-figure stat tiles. See `docs/decisions/0020-phase10l-institutional-refinement.md` for the full research citations and file/selector-level spec, with a companion style-tile Artifact. Ready for Phase 10m.
- [x] Phase 10m — Implement Interpretation, Institutional Refinement & Branding (`developer`) — DONE 2026-08-12: Built Phase 10k's Interpretation content and Phase 10l's spec into the React app. **Interpretation**: `app/api/interpretation.py` (pure function of `PortfolioAnalysis`, four always-present takeaways + dynamic headline + data-quality flags) wired into `POST /api/analysis` and rendered via `InterpretationSection.tsx` on Results — verified against live-computed data across three real portfolios, 48/48 tests passing, zero advice-language (grepped). **Institutional refinement**: builder credit in `AppSidebar.tsx`'s `SidebarFooter` (desktop + mobile Sheet) and `Overview.tsx`'s colophon, both linking `github.com/ethanverper/factor-lens`; h1 weight split (400 vs. h2-h4's 500); Goldman-style mono source citations under stat tiles; JPM-style footnote pattern (`FootnoteMarker` Popover) live on Learning's frontier/attribution cards; Learning rebuilt as a single-open `Accordion` with numbered/checkmarked lessons and a `Progress` bar persisted to `sessionStorage` (verified surviving a hard reload); predict-then-reveal interaction live on `CiWhiskerDiagram`; draggable-β slider live on `CapmDecompositionDiagram`, GSAP-animating the market-exposure block width. Verified: 76/76 backend tests, clean `tsc -b`/`npm run build`, live-browser pass across light/dark/375px mobile. Three judgment calls logged plainly (h1-weight scope, teaser/content overlap left as-is per real course-UI precedent, CI-whisker check reveals both rows' verdicts for pedagogical comparison). Ready for Phase 10n.
- [x] Phase 10n — Content Decomposition Specs (`educator` + `brand-creative`, split ownership) — DONE 2026-08-12: Sixth round of frontend feedback: even with Phase 10m's real components (Accordion, Cards) landed, the content *inside* them still read as walls of paragraph prose, and the Overview screen didn't reliably convey the full picture at a glance. Root-caused as a missing planning step between "content is written" and "developer builds the component" — added `docs/project-standards.md` rules 14 (30-second comprehension test for every project's landing screen) and 15 (content decomposition is a required planning step, split by content ownership), and installed the `information-architecture` skill. `educator` planned Learning's four cards (`docs/decisions/0021-phase10n-learning-content-decomposition.md`): lead→bullets→callout→pull-quote→worked-example per card, all worked examples re-verified live against `analyze_portfolio()` (not invented), new `LessonList`/`LessonCallout`/`LessonPullQuote`/`LessonExample` components reusing `InterpretationSection`'s existing visual language, and a reasoned split between always-visible `LessonCallout` (comprehension-critical caveats) vs. click-gated `FootnoteMarker` (optional-depth citations). `brand-creative` planned Overview/Real World/References (`docs/decisions/0022-phase10n-overview-and-content-decomposition.md`), live-audited first: Overview needed a "you provide" inputs chip row and method-card icons (not a rebuild); Real World was the worst offender (single-column prose stack, plus a real HTML-entity-escaping bug found while reading rendered output) — rewrote all four cards and moved to a 3-up grid plus a structurally distinct "spotlight" card; References' `legend` fields converted from run-on prose to real definition lists; audited Glossary and Tools & Technologies and found both genuinely already fine, no changes specified. New shared components spec'd: `Callout.tsx` and `Highlight.tsx`. Both agents flagged real judgment calls in their docs rather than guessing silently. Ready for Phase 10o.
- [x] Phase 10o — Implement Content Decomposition (`developer`) — DONE 2026-08-12: Built decisions 0021 and 0022 into the live app. New components: `LessonList`/`LessonCallout`/`LessonPullQuote`/`LessonExample` (reusing `InterpretationSection`'s flag-box and headline visual language, per 0021 §3) and `Callout`/`Highlight` (shared across Overview/Real World/References, per 0022). `learning.ts` restructured to the `LearningRegister` shape (lead/bullets/callout/pullQuote/footnote) verbatim per 0021 §4 for all four cards, both registers; the frontier/attribution notes moved off `FootnoteMarker` onto always-visible `LessonCallout`, two new genuine `FootnoteMarker` citations added (beta, frontier technical) pointing to decision 0003. Overview got the literal mechanism sentence, the "You provide" chip row, and `Target`/`Layers`/`LineChart` icons on the method cards. Real World's four cards rewritten to lead/bullets/quote/callout and the `stat`/`statLabel` entity-decoding bug fixed by writing literal Unicode characters in the data (simpler than widening `dangerouslySetInnerHTML` for two short fields) — **also found and fixed the same bug in `Highlight`'s `attribution` prop** (not caught by decision 0022's audit, live-verification turned it up: `&mdash; project roadmap` rendered undecoded) by applying the identical literal-character fix. References' `legend` fields converted to real `<dl>`s; Markowitz and ticker-universe cards' notes decomposed into lead/bullets/callout per 0022 §C, other cards left untouched. **QA item resolved**: live-measured the Real World 3-up grid at `lg` — cards came out 261px wide × 1190px tall (the shared `AppLayout` content column caps at `max-w-4xl`/896px regardless of viewport, so 3 columns never has room to breathe at any screen size) — switched to `lg:grid-cols-2` (400px cards, ~774–856px tall, much healthier ratio), third card sits alone in the grid's second row, spotlight card unaffected. Verified: 76/76 backend tests unchanged, clean `tsc -b`/`npm run build`, live-browser pass across all four touched pages in light/dark/375px mobile, footnote popovers and the moved callouts confirmed working, advice-language grep clean. See full report for the two minor deviations from the decision docs' literal component snippets (a broken dual `dangerouslySetInnerHTML`/`children` pattern in 0022's `Callout.tsx` example, simplified to plain children since callers already pass pre-rendered HTML spans). Ready for Phase 10j (QA sign-off).
- [x] Phase 10j — QA Sign-off on the Rebuild (`qa-tester`) — DONE 2026-08-12, **verdict: READY**: Full independent re-verification against `docs/project-standards.md` (not just this project's own definition of done) now that Phases 10i-10o have all landed. Backend 76/76, `tsc -b`/`npm run build` clean. Both new standard checks (rule 16's entity grep, the finance no-advice-language grep) came back clean — every entity match traced to a component that decodes it correctly, and the handful of "buy"/"rebalanc" hits are all legitimate (third-party description or explicit disclaimers). Live-tested two different real portfolios and confirmed the Interpretation headline demonstrably varies by branch; a single-asset AAPL 100% run incidentally triggered the "nothing stands out" default headline branch decision 0019 had never hit with real data. Server-side correctly rejected six adversarial `POST /api/analysis` payloads (invalid ticker/benchmark, an injection string, duplicate symbols, bad weight sum, empty holdings). All 8 sections, Learning's new structure, the required CI-whisker interaction, sessionStorage progress persistence, builder credit, and the rule-14 landing-screen test all confirmed live. Found and I fixed directly (small, mechanical, independently re-verified — tsc/build clean, no re-delegation needed): a `p=<0.001` double-operator bug (now a shared `pvalueLabel()` helper in `lib/format.ts` used everywhere a "p=" prefix was hand-concatenated), a "1 holdings" singular/plural bug in the frontier caption, and a frontier-chart marker-label clipping bug in narrow containers (`FrontierChart.tsx`'s `MarkerShape` now flips its text anchor when a marker sits in the right ~28% of the plotted domain, geometric rather than hardcoded to one label). Two interactions (the recommended draggable-β slider, the mobile Sheet) could not be exercised in either the QA agent's session or my own follow-up check — a reproducible Browser-tool click/drag timeout on that specific Radix control while the rest of the page stayed fully responsive; reads as a CDP-synthetic-pointer tooling artifact, not an app defect, but flagged as genuinely unconfirmed rather than assumed fine. Full report: `docs/status/2026-08-12-phase10j-qa-signoff.md`. Ready for Ethan's review before Phase 11.
- [x] Phase 10p — Interpretation & Key Takeaways Content Decomposition (direct fix, no delegation) — DONE 2026-08-12: Ethan caught, live in the deployed app, exactly the gap `docs/project-standards.md` rule 15's own change log now documents: the Results page's Interpretation section — four takeaway cards — was still one long paragraph each, missed by both the Phase 10n sweep and the Phase 10j QA pass because rule 15's ownership line never named an owner for rule-9a content. Restructured `app/api/interpretation.py`: each of the four `_xxx_body` functions now returns a `TakeawayBody(lead, bullets)` instead of a single joined string — the exact same analytical sentences, same thresholds, same numbers, purely re-chunked into a lead sentence plus a real bullet list; a `.body` compat property (join of lead+bullets) kept all ~20 existing substring-assertion tests passing unchanged. Threaded through `InterpretationTakeaway` (`app/api/schemas.py`, `frontend/src/lib/types.ts`) and `InterpretationSection.tsx`, which now renders `lead` as the topic sentence and `bullets` via the already-existing `LessonList` component — no new UI primitive needed. While live-verifying, caught two real regressions/bugs, both fixed and re-verified: (1) the `pvalueLabel()` helper added in Phase 10j's own bug-fix pass had a latent double-operator bug of its own (`p<<0.001` — `fmtPvalue()` already prepends "<", `pvalueLabel` prepended a second one; never caught because Phase 10j's own live-check happened to land on a p-value that didn't hit the `p<0.001` branch), now fixed to own the operator in exactly one place; (2) a second, independent instance of the identical anti-pattern in the *backend's* own prose generation (`interpretation.py`'s `_fmt_p` had the same bug, in three places, since it predates and is a different file from the frontend fix) — renamed to `_fmt_p_label` and fixed the same way. Fixed the actual rule-15 ownership gap in `docs/project-standards.md` (rule 15 now says "whichever agent rule 9a assigns content to," not just "`educator`/`brand-creative`") and added the matching note to `quant-analyst.md`, so this doesn't recur on the next finance project. Verified: 76/76 backend tests, clean `tsc -b`/`npm run build`, live-browser re-check of the Results page confirming both the bullet structure and the `p<0.001` fix render correctly. Ready for Ethan's review before Phase 11.

- [~] Phase 11 — Deploy (`devops`, gated by Ethan's explicit go-ahead, given 2026-08-12): **Deployment config complete and verified; live Railway connection blocked on Ethan's own login step.** Built `Dockerfile` (multi-stage: Node builds `frontend/dist/`, then a `uv`-managed Python 3.11 stage installs deps and serves the single FastAPI process — the one-service shape decision 0018 §2 already committed to), `railway.json` (`DOCKERFILE` builder, `/health` healthcheck, restart-on-failure), and `.dockerignore`. Confirmed no environment variables are required (grepped `app/` for any `os.environ`/`getenv`/API-key usage — none; both OpenBB's `yfinance` provider and Kenneth French's Data Library via `pandas-datareader` are keyless, per decision 0002). Baked OpenBB's one-time generated-package-tree build (decision 0014) into the image at build time so it doesn't tax the health check on first container start. Verified the exact production shape locally end-to-end (not just that the build succeeds): built `frontend/dist/` fresh, ran `uv run uvicorn app.main:app --host 0.0.0.0 --port $PORT`-style on an arbitrary port, confirmed `/health`/`/`/deep-link `/results`/`/api/*`/built assets all 200, ran a live `POST /api/analysis` against real yfinance/Kenneth French data, and drove the running app through Playwright (Overview's live mini-frontier preview, the sample quick-start's shareable-link URL mechanism, the full Results page including a real, non-generic Interpretation & Key Takeaways headline). **What's blocked, and why**: creating/signing into a Railway account and authorizing Railway's GitHub App against `github.com/ethanverper/factor-lens` are both interactive login/OAuth steps — per `devops`'s standing mandate, those are Ethan's to complete himself, never attempted here. Also flagged, per the standing "never provision paid resources without asking every time" rule: Railway's current pricing (checked live) is a 30-day/$5-credit no-card-required trial, after which continued hosting needs an explicit Hobby-plan ($5/mo) call from Ethan, not an assumption. See `docs/decisions/0023-phase11-railway-deployment.md` for the full config rationale and the exact handoff steps. **Once Ethan completes the Railway connection and shares the resulting URL, remaining work is live-URL re-verification and the README/roadmap link update — everything else is done.**

Later (not v1, tracked here for continuity per the idea's own scalability case — not scheduled):
- v2 — multi-portfolio / household view for RIAs.
- v3 — B2B API product (portfolio-optimization/factor-attribution engine other fintech apps embed) — the research brief explicitly flags this as unexplored territory.

## RAID

**Risks:**
- The "explainability" differentiator is easy to claim and hard to defend durably — incumbents with existing distribution and data (Composer, QuantConnect, YCharts post-Zephyr acquisition) could bolt on similar attribution views quickly.
- Thin demand signal risk: the research brief's "thin coverage" of retail/small-team factor tooling may reflect thin *demand*, not just thin *supply* — worth a demand check before investing beyond v1.
- OpenBB Open Data Platform is a young, actively-changing dependency (v4.5 just shipped); provider/API surface changes could break the data integration mid-build.

**Assumptions:**
- OpenBB's free tier / Open Data Platform provides sufficient live equity price history and Fama-French factor return series for v1 scope without requiring a paid data subscription. **Confirmed in Phase 1 for equity/benchmark prices (OpenBB + yfinance provider, no key needed), with one correction: OpenBB's Open Data Platform does not carry Fama-French factor series at all — sourced instead from Kenneth French's Data Library directly via `pandas-datareader`, also free/no key needed. See `docs/decisions/0002-phase1-stack-and-data-sourcing.md`.**
- The "retail investor / small RIA" target persona is validated only against desk research (the source brief), not primary user interviews — v1 proceeds on that basis; a demand check is a recommended gate before committing to v2.

**Dependencies (internal, sequential):**
- Phase 2 depends on Phase 1's OpenBB integration returning clean, structured data.
- Phase 3 depends on Phase 2's model outputs being stable and structured.
- Phase 4 depends on both Phase 2 (numbers) and Phase 3 (visual framing) being in place to narrate against.
- Phase 5 depends on Phases 1–4 being functionally complete for end-to-end testing.

**Explicitly low risk (noted, not tracked as an open item):**
- No `legal-compliance` or custody review required. This is pure decision-support analytics — no trade execution, no custody of client funds, no regulated disclosure output — consistent with both the idea's own feasibility read and the source research brief's scope exclusion ("excludes anything requiring a banking license, custody of client funds, or large capital"). Contrast with the shortlist's `Ledger & Ruin` idea, which was explicitly held back for this exact reason.
